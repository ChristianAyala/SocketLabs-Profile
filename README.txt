The profile that goes along with the SocketLabs feature.
When using the makefile located in the SocketLabs repo, this
profile will automatically be downloaded and inserted into the
profiles folder in the Drupal install location. You can then
select this profile in the installation sequence, and the
Socketlabs feature will be added to the core install sequence.
